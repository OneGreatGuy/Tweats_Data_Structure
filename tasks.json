{
        "version": "2.0.0",
        "tasks": [
            {
                "label": "make project",
                "type": "shell",
                "command": "cd ${workspaceFolder} && rm run_app && rm -rf ${workspaceFolder}/.vscode/ipch && make",
                "group": {
                    "kind": "build",
                    "isDefault": true
                }
            }
        ]
    }